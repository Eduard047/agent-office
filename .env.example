# Optional: only needed for a hosted API-backed deployment.
# Local development uses Codex CLI signed in through ChatGPT.
OPENAI_API_KEY=

# Optional overrides. Defaults are chosen to keep routine runs compact.
OPENAI_ORCHESTRATOR_MODEL=gpt-5.6-terra
OPENAI_WORKER_MODEL=gpt-5.6-luna
